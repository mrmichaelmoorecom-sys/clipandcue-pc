{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dropdown window",
  "windows": ["dropdown"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "opener:default"
  ]
}
